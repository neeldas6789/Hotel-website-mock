target:
  - url: "docs/" # folder where docs will be generated

include:
  - src/ # specify which folders/files to include

exclude:
  - node_modules/ # skip heavy/unnecessary dirs

reinit: "some-random-string" # unique string to force reinitialization
